require 'spec_helper'

describe UserDetail do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    UserDetail.create!(@valid_attributes)
  end
end
